# .claude/

Claude Code reads project instructions from `CLAUDE.md` in the repository root, not from this
directory, so that is where they are. It points at [AGENTS.md](../AGENTS.md), which is the one
file every assistant reads.

This directory is for Claude Code's own project configuration — `settings.json`, `commands/`,
`agents/`, `skills/` — and holds nothing yet.
